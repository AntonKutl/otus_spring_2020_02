
spring:
  datasource:
    initialization-mode: always
    schema: schema.sql
    data: data.sql

  banner:
    charset: UTF-8
    location: classpath:banner.txt

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

